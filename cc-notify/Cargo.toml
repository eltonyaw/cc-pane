[package]
name = "cc-notify"
version = "0.1.0"
edition = "2021"
description = "Push notification system for CC-Panes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = "3"
